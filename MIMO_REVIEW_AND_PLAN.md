# MIMO — Final Version 리뷰 리포트

이전 세션에서 P1~P7(실제 지도, 위치 권한, 카드 개선, Merchant, Admin, 예약 자동화, 알림)을
이미 구현해 배포까지 완료한 상태에서, 이번 "Final Version" 지침을 기준으로 실제 코드를
다시 검토한 결과입니다. 코딩은 이 리포트 이후에 시작합니다.

## 1. 현재 아키텍처

React 19 + Vite + TypeScript SPA, react-router-dom v7. 상태는 용도별로 4개 컨텍스트로 분리돼
있습니다: `MimoDataContext`(소비자 앱: salons/user/reservations/notifications),
`LocationContext`(Geolocation), `MerchantDataContext`(사장님 앱), `AdminDataContext`(관리자).
지도는 `@vis.gl/react-google-maps`, 백엔드는 Supabase(`mimo_salons`,`mimo_users`,
`mimo_reservations`,`mimo_notifications`,`mimo_reviews`,`mimo_reports`). 라우트는 `/mimo`
(소비자), `/merchant`, `/admin`으로 분리되고 코드 스플리팅 적용됨.

## 2. 부족한 부분 (Weak Points)

- **지도-리스트 연동이 단방향**: 핀 선택 → 카드 강조는 되지만, 카드 선택 → 핀 강조는 안 됨.
  카드를 누르면 곧바로 상세 페이지로 이동해버려서 "홈에서 훑어보기"가 불가능합니다.
- **지도 이동 시 자동 갱신 없음**: 지도를 드래그해도 리스트는 그대로 전체 매장을 보여줍니다.
- **지도가 화면의 중심이 아님**: `MimoHome`에서 지도 높이가 `h-64`(256px)로 고정되어 있어
  요청하신 "지도 비중 확대"와 반대 방향입니다.
- **카드가 사진 중심**: 80×80 썸네일이 좌측에 고정 배치되고 "지금 예약 가능" 배지는 작은
  텍스트라, "정보 전달이 사진보다 우선"이라는 요구와 어긋납니다. 시술 소요시간도 카드에 없음.
- **실시간 동기화 없음**: Supabase Realtime을 전혀 쓰지 않아서, A 사용자가 결제하거나
  사장님이 OFF를 눌러도 B 사용자의 화면은 새로고침 전까지 갱신되지 않습니다.
- **예약완료 화면에 액션 없음**: 길찾기/전화하기/예약취소 버튼이 없고, 애초에 `MimoSalon`
  타입에 전화번호 필드도 없습니다.
- **리뷰 작성 기능 없음**: Admin에서 리뷰를 조회/삭제할 수는 있지만, 사용자가 리뷰를
  남기는 화면 자체가 존재하지 않습니다.
- **ETA/소요시간 미표시**: 도보 시간만 계산하고 자동차 예상 시간은 없음. 시술 소요시간은
  상세 페이지에만 있고 홈 카드에는 없음.
- **이미지 lazy loading 없음**: 모든 `<img>`가 즉시 로드됩니다.

## 3. UI 불일치

- 카드 레이아웃의 시각적 위계가 지침과 반대(사진 > 상태 배지) — 텍스트/상태 우선으로 뒤집어야 함.
- "매장을 훑어보는" 지도 중심 화면인데 실제로는 리스트 중심 레이아웃(지도가 화면 상단의
  작은 위젯처럼 보임).
- 카드에 select 전용 탭 영역이 없어 Apple Maps식 "가볍게 선택 → 필요하면 진입" 흐름이 아님.

## 4. UX 개선 필요 사항

- 지도-카드 양방향 연동 + 뷰포트 기준 리스트 필터링.
- 홈 레이아웃을 지도 중심(상단 대형 지도 + 하단 카드 시트)으로 재구성.
- 서비스가 1개뿐인 매장은 상세 페이지를 건너뛰고 바로 결제로 진입(3탭 목표에 근접).
- 예약완료 화면에 길찾기/전화/취소 추가.
- 완료된 예약에 대해 별점+한줄 리뷰(선택) 작성 유도.

## 5. 성능 이슈

- 홈 진입 시 필요한 메인 번들이 여전히 gzip ~250KB (지도 래퍼 + framer-motion 비중 큼).
- 이미지 lazy loading 미적용으로 스크롤 초기 로딩에 불필요한 네트워크 부하.
- 실시간 동기화가 없어 사용자가 "새로고침"에 의존 → 체감 속도/신뢰도 저하.

## 6. 보안 이슈 (이전 리포트에서 이어짐, 아직 미해결)

- 로그인이 진짜 OAuth가 아니라 localStorage uid 기반 — Merchant/Admin 권한도 클라이언트
  사이드 체크(소유권 매칭, 패스코드)라 개발자 도구로 우회 가능.
- 이번 세션에서도 Supabase에 직접 접근할 수 없어 실제 RLS 정책을 확인하지 못했습니다.
- 매장 ON/OFF 전환이 여전히 클라이언트에서 순차 처리되어 동시 결제 경쟁 상태 가능성 존재.

## 7. 권장 구현 순서 (이번 세션에서 적용)

1. 홈 레이아웃 재구성 (지도 중심 + 카드 시트) — 이후 모든 UX의 기반
2. 카드 디자인 재설계 (정보 우선, "지금 가능" 강조, 소요시간 표시)
3. 지도-카드 양방향 연동 + 뷰포트 기준 리스트 필터링
4. 예약 플로우 단축 (단일 서비스 매장은 상세 생략)
5. Supabase Realtime 연동 (매장 상태 실시간 반영)
6. 예약완료 화면 액션(길찾기/전화/취소) + 매장 전화번호 필드
7. 리뷰 작성 플로우 (별점 + 한줄, 완료된 예약 대상)
8. 도보/자동차 ETA + 시술 소요시간 카드 노출
9. 이미지 lazy loading + Admin 매장 관리(강제 OFF) 보강

기존 UI 철학(미니멀, 화이트, 라운드 카드, Apple HIG)과 라우트/컨텍스트 구조는 그대로
유지하고, 위 항목은 모두 기존 컴포넌트를 개선하는 방식으로 적용합니다.
